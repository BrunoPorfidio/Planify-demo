import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.planifyapplucia',
  appName: 'Planify - App Lucia',
  webDir: 'dist'
};

export default config;
